
# QEMU Configuration for macOS
# Use TCG acceleration instead of KVM

machine {
    type = "pc"
    accel = "tcg"
    cpu = "host"
}

memory {
    size = "2048"
}

disk {
    format = "qcow2"
    cache = "writeback"
}

network {
    type = "user"
}
